# Naive RAG with RAGAS Evaluation
# Educational Project - Bitcoin Whitepaper
# 100% Local Infrastructure (TEI + Claudex/Ollama)

# Core LLM Framework
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-ollama>=0.3.0

# Document Processing
pypdf>=4.0.0
langchain-text-splitters>=0.3.0

# Vector Store - FAISS (default, requires TEI server)
faiss-cpu>=1.7.4

# Vector Store - Qdrant (Mac Silicon alternative, no server needed)
langchain-qdrant>=0.2.0
qdrant-client>=1.7.0

# Local Embeddings (Mac Silicon compatible - alternative to TEI)
langchain-huggingface>=0.1.0
sentence-transformers>=2.2.0

# RAGAS Evaluation
ragas>=0.2.0

# Data Handling
pandas>=2.0.0
datasets>=2.0.0

# HTTP Client (for TEI)
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.0
numpy>=1.24.0
